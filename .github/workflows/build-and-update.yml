name: Build image and update GitOps repo
on:
  push:
    branches: [ main ]

jobs:
  build-and-update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Log in to ACR
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.REGISTRY_LOGIN_SERVER }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build distroless image and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ secrets.REGISTRY_LOGIN_SERVER }}/myapp:${{ github.sha }}

      # - name: Check out GitOps repo
      #   uses: actions/checkout@v4
      #   with:
      #     repository: <your-org>/myapp-gitops
      #     token: ${{ secrets.GITOPS_REPO_TOKEN }}
      #     path: gitops

      # - name: Update image tag and commit
      #   working-directory: gitops/apps/myapp
      #   run: |
      #     kustomize edit set image myapp=${{ secrets.REGISTRY_LOGIN_SERVER }}/myapp:${{ github.sha }}
      #     git config user.name  "CI Bot"
      #     git config user.email "ci@example.com"
      #     git commit -am "Deploy myapp:${{ github.sha }}"
      #     git push
